{
  "name": "peakapp",
  "version": "1.0.0",
  "description": "Metabolomics Feature Viewer",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "vite": "^7.3.1"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}
